const gulp = require('gulp');

// Define a task to build your ESLint rules
gulp.task('build-eslint-rules', function() {
    return new Promise((resolve, reject) => {
        // Your build process here
        resolve();
      });
});
